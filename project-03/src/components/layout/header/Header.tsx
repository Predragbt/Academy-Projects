import { BottomHeader } from "./BottomHeader";
import { TopHeader } from "./TopHeader";

export const Header = () => {
  return (
    <header>
      <TopHeader />
      <BottomHeader />
    </header>
  );
};
